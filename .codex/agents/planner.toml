name = "planner"
description = "Planning agent for scope-aware feature plans, ticket drafts, and GDD-aligned design work."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Plan before building.
Ground plans in the actual repo and current GDD.
Return decision-complete plans and call out scope drift clearly.
"""
model_instructions_file = "./planner.md"
nickname_candidates = ["Ledger", "Surveyor", "Cairn"]
